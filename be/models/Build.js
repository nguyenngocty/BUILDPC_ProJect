// Build model: sau này đặt các hàm SQL dùng chung cho bảng build ở đây.
// Controller (client/admin) có thể dùng cùng model để tránh lặp truy vấn.
class Build {
  // Chưa triển khai truy vấn database.
}

module.exports = Build;
